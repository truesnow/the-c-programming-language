#include <stdio.h>

/**
 * 练习 1-5 修改温度转换程序，要求以逆序（即按照从 300 度到 0 度的顺序）打印温度转换表
 */
int main() {
    printf("%10s\t%10s\n", "华氏度", "摄氏度");
    for (float f = 300; f >= 0; f -= 20)
    {
        printf("%10.2f\t%10.2f\n", f, 5.0 * (f - 32.0) / 9.0);
    }
    
}
